{
  "connections": [
    { "slug": "deepseek", "providerType": "deepseek", "defaultModel": "deepseek-chat", "apiKeyEnv": "DEEPSEEK_API_KEY" }
  ],
  "configs": [
    { "id": "deepseek-chat", "backend": "ai-sdk", "llmConnectionSlug": "deepseek", "model": "deepseek-chat" }
  ],
  "tasks": [
    {
      "id": "fix-add",
      "instruction": "The test in test.mjs is failing because of a bug in src.mjs. Read both files, fix the bug in src.mjs so the test passes, then run `node --test` to confirm. Only edit src.mjs.",
      "workspaceDir": "./fix-add",
      "verification": { "command": "node --test", "timeoutMs": 120000 }
    }
  ]
}
